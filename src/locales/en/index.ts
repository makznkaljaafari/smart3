import { enCommon } from './common';
import { enDashboard } from './dashboard';
import { enFeatures } from './features';
import { enSettings } from './settings';
import { enTeam } from './team';

export const en = {
  ...enCommon,
  ...enDashboard,
  ...enFeatures,
  ...enSettings,
  ...enTeam,
};